POSTGRES_USER=plantpulse
POSTGRES_PASSWORD=changeme
POSTGRES_DB=plantpulse

# Used by tools running on YOUR machine (alembic, local scripts) —
# Postgres is reachable at localhost because Docker Compose publishes
# the port to the host.
DATABASE_URL=postgresql+psycopg://plantpulse:changeme@localhost:5432/plantpulse